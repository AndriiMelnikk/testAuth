export enum StatusChallenge {
  active = 'active',
  archive = 'archive',
  delete = 'delete',
  wait = 'wait',
}

export enum ActiveChallenge {
  active = 'active',
  steps = 'steps',
  training = 'training',
}
